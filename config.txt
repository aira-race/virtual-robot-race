# Virtual Robot Race Beta Configuration File (config.txt)
# Global settings - which robots to activate

# Hostname or IP address where Unity will connect
HOST=localhost

# WebSocket port number
PORT=12346

# Active robots (comma-separated: 1,2)
# Beta version supports up to 2 robots maximum
# Example: ACTIVE_ROBOTS=1      → Robot1 only
# Example: ACTIVE_ROBOTS=1,2    → Robot1, Robot2
ACTIVE_ROBOTS=1,2

# Per-robot settings are managed in Robot1/robot_config.txt, Robot2/robot_config.txt, etc.

# Unity launch mode (advanced setting):
# 0 = Auto-launch Unity (recommended)
# 1 = Manual launch (for debugging)
DEBUG_MODE=0