# AAGP Configuration File (config.txt)
# 全体設定 - どのロボットをアクティブにするか

# Hostname or IP address where Unity will connect
HOST=localhost

# WebSocket port number
PORT=12346

# アクティブなロボット (カンマ区切りで指定: 1,2,3,4,5)
# 例: ACTIVE_ROBOTS=1      → Robot1のみ
# 例: ACTIVE_ROBOTS=1,3,5  → Robot1, Robot3, Robot5
ACTIVE_ROBOTS=1

# 各ロボットの設定は Robot1/robot_config.txt などで個別に管理

# Unity launch mode:
# 0 = Launch Unity automatically from Unity_Build.exe
# 1 = Launch Unity manually (useful for debugging)
DEBUG_MODE=1
