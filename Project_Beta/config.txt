# AAGP Configuration File (config.txt)
# Global settings - which robots to activate

# Hostname or IP address where Unity will connect
HOST=localhost

# WebSocket port number
PORT=12346

# Active robots (comma-separated: 1,2,3,4,5)
# Example: ACTIVE_ROBOTS=1      → Robot1 only
# Example: ACTIVE_ROBOTS=1,3,5  → Robot1, Robot3, Robot5
ACTIVE_ROBOTS=1,2

# Per-robot settings are managed in Robot1/robot_config.txt, Robot2/robot_config.txt, etc.

# Unity launch mode:
# 0 = Launch Unity automatically from Unity_Build.exe
# 1 = Launch Unity manually (useful for debugging)
DEBUG_MODE=0