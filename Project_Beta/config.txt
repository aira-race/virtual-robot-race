# AAGP Configuration File (config.txt)

# Hostname or IP address where Unity will connect
HOST=localhost

# WebSocket port number
PORT=12346

# Execution mode:
# 1 = keyboard (manual driving)
# 2 = table (CSV playback)
# 3 = rule_based (rule-based logic control)
# 4 = ai (use AI model inference)
MODE_NUM=1

# Unity launch mode:
# 0 = Launch Unity automatically from AAGP.exe
# 1 = Launch Unity manually (useful for debugging)
DEBUG_MODE=1

# JPEG image saving:
# 0 = Delete images after run (lightweight mode)
# 1 = Save images for AI training
JPEG_SAVE=0

# Player settings
# NAME: Player name (up to 10 characters, alphanumeric only)
# RACE_FLAG: Participation flag (1=Participate, 0=Do not participate)
NAME=AndyC
RACE_FLAG=0

# --- post race video options ---
AUTO_MAKE_VIDEO=0          # 1=auto make mp4, 0=off
OPEN_EXPLORER_ON_VIDEO=1   # 1=open Explorer on finish (Windows)
VIDEO_FPS=20               # default FPS for MP4
INFER_FPS=1                # 1=estimate FPS from timestamps
